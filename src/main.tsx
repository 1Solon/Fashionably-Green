import React from 'react'
import ReactDOM from 'react-dom/client'
import Paperbase from './Paperbase.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Paperbase />
  </React.StrictMode>,
)
